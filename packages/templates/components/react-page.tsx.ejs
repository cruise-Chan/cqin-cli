<% if (language === 'TypeScript') { %>import { FC } from 'react'<% } %>
<% if (needRouter) { %>import { Link } from 'react-router-dom'<% } %>
import './<%= name %>.<%= styleExt %>'

<% if (language === 'TypeScript') { %>
interface Props {
  // 示例props
  className?: string
}

const <%= name %>: FC<Props> = ({ className }) => {
<% } else { %>
const <%= name %> = () => {
<% } %>
  return (
    <div className="<%= name.toLowerCase() %>-container">
      <h1>Welcome to <%= name %></h1>
      <% if (needRouter) { %>
      <nav>
        <Link to="/<%= name === 'Home' ? 'about' : '' %>">
          Go to <%= name === 'Home' ? 'About' : 'Home' %>
        </Link>
      </nav>
      <% } %>
    </div>
  )
}

export default <%= name %>