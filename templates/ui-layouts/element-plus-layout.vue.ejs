<template>
  <el-container class="h-screen">
    <!-- 侧边栏 -->
    <el-aside width="200px" class="bg-gray-100">
      <div class="logo p-4">Logo</div>
      <el-menu router :default-active="$route.path" class="border-none">
        <el-menu-item index="/home">
          <el-icon><House /></el-icon>
          <span>首页</span>
        </el-menu-item>
        <el-menu-item index="/about">
          <el-icon><InfoFilled /></el-icon>
          <span>关于</span>
        </el-menu-item>
      </el-menu>
    </el-aside>

    <el-container>
      <!-- 顶部导航 -->
      <el-header class="flex items-center border-b">
        <div class="flex-1">欢迎使用系统</div>
        <el-dropdown>
          <span class="el-dropdown-link">
            <el-avatar icon="UserFilled" />
          </span>
          <template #dropdown>
            <el-dropdown-menu>
              <el-dropdown-item>个人中心</el-dropdown-item>
              <el-dropdown-item>退出登录</el-dropdown-item>
            </el-dropdown-menu>
          </template>
        </el-dropdown>
      </el-header>

      <!-- 主要内容 -->
      <el-main>
        <router-view />
      </el-main>

      <!-- 底部信息 -->
      <el-footer class="text-center text-sm text-gray-500">
        © 2024 Your Company. All rights reserved.
      </el-footer>
    </el-container>
  </el-container>
</template>

<script setup lang="ts">
import { House, InfoFilled, UserFilled } from "@element-plus/icons-vue";
</script>

<style <% if (cssPreprocessor !== 'CSS') { %>lang="<%= styleLang %>"<% } %>>
<%_ if (cssPreprocessor === 'Sass/SCSS') { _%>
$primary-color: #409eff;
$sidebar-bg: #304156;
$header-bg: #ffffff;
$text-color: #2c3e50;

.el-container {
  height: 100vh;
  
  .el-aside {
    background: $sidebar-bg;
    transition: width 0.3s;
    
    .logo {
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: $primary-color;
      font-size: 20px;
      font-weight: 600;
      background: $sidebar-bg;
    }

    .el-menu {
      border-right: none;
      background: transparent;
      
      :deep(.el-menu-item) {
        color: rgba(255,255,255,0.7);
        transition: all 0.3s;
        
        &.is-active {
          color: $primary-color;
          background: rgba(255,255,255,0.05) !important;
        }
        
        &:hover {
          background: rgba(255,255,255,0.05);
        }
        
        .el-icon {
          margin-right: 8px;
          font-size: 18px;
        }
      }
    }
  }

  .el-header {
    background: $header-bg;
    box-shadow: 0 1px 4px rgba(0,21,41,.08);
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: $text-color;
    
    .el-dropdown-link {
      display: flex;
      align-items: center;
      cursor: pointer;
    }
  }

  .el-main {
    padding: 20px;
    background: #f0f2f5;
  }

  .el-footer {
    display: flex;
    align-items: center;
    justify-content: center;
    background: $header-bg;
    color: rgba($text-color, 0.65);
    box-shadow: 0 -1px 4px rgba(0,21,41,.08);
  }
}

<%_ } _%>

<%_ if (cssPreprocessor === 'Less') { _%>
@primary-color: #409eff;
@sidebar-bg: #304156;
@header-bg: #ffffff;
@text-color: #2c3e50;

.el-container {
  height: 100vh;
  
  .el-aside {
    background: @sidebar-bg;
    transition: width 0.3s;
    
    .logo {
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: @primary-color;
      font-size: 20px;
      font-weight: 600;
      background: @sidebar-bg;
    }

    .el-menu {
      border-right: none;
      background: transparent;
      
      :deep(.el-menu-item) {
        color: rgba(255,255,255,0.7);
        transition: all 0.3s;
        
        &.is-active {
          color: @primary-color;
          background: rgba(255,255,255,0.05) !important;
        }
        
        &:hover {
          background: rgba(255,255,255,0.05);
        }
        
        .el-icon {
          margin-right: 8px;
          font-size: 18px;
        }
      }
    }
  }

  .el-header {
    background: @header-bg;
    box-shadow: 0 1px 4px rgba(0,21,41,.08);
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: @text-color;
    
    .el-dropdown-link {
      display: flex;
      align-items: center;
      cursor: pointer;
    }
  }

  .el-main {
    padding: 20px;
    background: #f0f2f5;
  }

  .el-footer {
    display: flex;
    align-items: center;
    justify-content: center;
    background: @header-bg;
    color: rgba(@text-color, 0.65);
    box-shadow: 0 -1px 4px rgba(0,21,41,.08);
  }
}
<%_ } _%>

<%_ if (cssPreprocessor === 'Stylus') { _%>
$primary-color = #409eff
$sidebar-bg = #304156
$header-bg = #ffffff
$text-color = #2c3e50

.el-container
  height 100vh
  
  .el-aside
    background $sidebar-bg
    transition width 0.3s
    
    .logo
      height 60px
      display flex
      align-items center
      justify-content center
      color lighten($primary-color, 10%)
      font-size 20px
      font-weight 600
      background darken($sidebar-bg, 5%)

    .el-menu
      border-right none
      background transparent
      
      :deep(.el-menu-item)
        color rgba(255,255,255,0.7)
        transition all 0.3s
        
        &.is-active
          color $primary-color
          background rgba(255,255,255,0.05) !important
        
        &:hover
          background rgba(255,255,255,0.05)
        
        .el-icon
          margin-right 8px
          font-size 18px

  .el-header
    background $header-bg
    box-shadow 0 1px 4px rgba(0,21,41,.08)
    display flex
    align-items center
    justify-content space-between
    color $text-color
    
    .el-dropdown-link
      display flex
      align-items center
      cursor pointer

  .el-main
    padding 20px
    background #f0f2f5

  .el-footer
    display flex
    align-items center
    justify-content center
    background $header-bg
    color rgba($text-color, 0.65)
    box-shadow 0 -1px 4px rgba(0,21,41,.08)
<%_ } _%>

</style>
